import pandas as pd
from datetime import datetime
import dateutil.relativedelta as relativedelta
first_date = datetime.strptime(input(), '%Y-%m-%d %H:%M:%S').strftime("%Y-%m-%d")
last_date  = (pd.to_datetime(input())-relativedelta.relativedelta(days=1)).strftime("%Y-%m-%d")

def reading_data_csv (file):
    df=pd.read_csv(file)

    print(df[(df['date']>=first_date) &(df['date']<last_date)])
reading_data_csv(input())
